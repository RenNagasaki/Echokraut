// Copy this file to DriveAuthProvider.Secrets.cs and fill in your credentials.
// DriveAuthProvider.Secrets.cs is gitignored and must never be committed.

namespace Echokraut.Services;

internal sealed partial class DriveAuthProvider
{
    private const string CLIENTID = "";
    private const string CLIENTSECRET = "";
}

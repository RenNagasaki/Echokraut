// Copy this file to GoogleDriveSyncService.Secrets.cs and fill in your credentials.
// GoogleDriveSyncService.Secrets.cs is gitignored and must never be committed.

namespace Echokraut.Services;

public partial class GoogleDriveSyncService
{
    private const string CLIENTID = "";
    private const string CLIENTSECRET = "";
}
